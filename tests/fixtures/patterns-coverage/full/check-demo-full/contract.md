# check-demo-full — Output Contract

Fixture contract for the patterns-coverage gate. Both declared categories are
emitted by the sibling patterns.sh, so this domain scores 2/2 (100%).

## Categories

| Category         | Certainty | Method        | Confidence |
| ---------------- | --------- | ------------- | ---------- |
| `alpha-finding`  | HIGH      | deterministic | >= 0.9     |
| `beta-finding`   | HIGH      | deterministic | >= 0.9     |

## Finding Format

Standard finding-schema.md. This section deliberately mentions no category
slugs so the extractor only picks up the table above.
