# check-security — Output Contract (fixture stub)

## Language Support

The table below is NOT wrapped in a `<!-- contract: -->` marker, so it does not
resolve. A heading-anchored gate would happily find this section and pass; an
id-anchored one must report the scanner as unmatched. That difference is the
whole point of the marker convention (see tests/lib/harness.sh).

| Language | ext(s) | some-category |
| -------- | ------ | ------------- |
| Python   | py     | M             |
