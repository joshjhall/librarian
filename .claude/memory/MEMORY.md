# Memory index

<!-- One line per memory (title + one-line hook), intentionally long. -->
<!-- rumdl-disable MD013 -->

- [Stale-BLOCKED false positive](stale-blocked-false-positive.md) — golem-status BLOCKED pins send-keys-resolved plan gates for the full 3600s TTL (no clearing feed line emitted on send-keys) → trains you to ignore the list → nearly miss a REAL fresh gate (405's review-workflow-run prompt); host command-center/feed was RIGHT, my pane-grep was WRONG (matched "tokens" in gate boilerplate); filed #422; trust feed/pane-FOOTER over pane-grep
- [check-docs-staleness IFS=: colon parity](check-docs-staleness-ifs-colon-parity.md) — latent bash↔python bug: `while IFS=: read -r line_num content` strips a TRAILING colon from evidence that the python primary keeps; hidden until #397's test comment (version-ref line ending in `:`) became the first repo file to trip it; validate-prescan-differential.sh (whole-tree) caught it; reworded to unblock, underlying patterns.sh bug needs a follow-up fix
- [Git index corruption → partial commit](git-index-corruption-partial-commit.md) — a truncated .git/index made `git add`+commit capture only 1 of ~384 files with NO error (PR #403, #238); working tree was fine; recover via read-tree last-good + reset --mixed + re-stage + force-with-lease; always verify `gh pr view --json files` post-push
- [#283 checkpoint table](issue-283-checkpoint-table.md) — SHIPPED PR #414 (2026-07-19): golem-status.sh --checkpoint compact per-track status+burn table; reuses #371 token scrape + `started`, no schema change; --checkpoint REPLACES verbose (shared scrape_and_persist_tokens helper), count-drop=reset excluded from Δ, numeric-guard persisted prior (octal/overflow); tracks.json join + glob-exclusion latent bug; 3-cycle adversarial review (2 blocking rounds); DURABLE FIX = run-all.sh unsets GIT_DIR at entry point (kills the pre-push hook-leak flake class); follow-up #415
- [#256 cache-stability pass](issue-256-cache-stability.md) — SHIPPED PR #404 (2026-07-18): workflow.js prompt-prefix cache stability; stableStringify (key-sort, array-order-preserving) via dataBlock in 3 byte-compat siblings + instructions-first/volatile-last reorder + hoisted menus; KEY INSIGHT = big always-free prefix is agent system+tools (harness never touches), harness can't place cache_control breakpoint, so reorder payoff is runtime-dependent/cross-cycle only; helper-test slices at ORCH_BOUNDARY so new helpers must be module-scope
- [Orchestrate session handoff](orchestrate-session-handoff.md) — batch-9 COMPLETE (2026-07-18): 11 trivial/small follow-ups shipped + #348 umbrella CLOSED (9 patterns.py ports 100% via slices #383/#389); #390 open (Mode-3 token push), #227 HELD for interactive; #371 token-counter last-in-flight; golem-launch hardcodes --level 4 (hand-roll L3); classifier gates dynamic shell even in auto mode; next = #227 interactive + large/feature backlog (#248/#256/#283/#284/#343)
- [Verify squash-merge landed](verify-squash-merge-landed.md) — after merging a PR, confirm the diff actually landed on origin/main, not just the title
- [Auto-mode blocks self-merge](auto-mode-blocks-self-merge.md) — L3/L4 ship `gh pr merge` is denied by the auto-mode classifier (self-authored, no human approval); park for human merge
- [Stale-base squash reverts merged PR](stale-base-squash-reverts-merged-pr.md) — `reset --soft origin/main` from a worktree branched before a mid-session merge silently reverts that PR's files; restore each advanced file before amending
- [Ship worktree merge cleanup](ship-worktree-merge-cleanup.md) — `gh pr merge --delete-branch` from a worktree fails local checkout ('main' used elsewhere) but the remote merge lands; verify + finish cleanup manually, don't treat as dead-end
- [Orchestrate broker-then-send](orchestrate-broker-then-send.md) — at a plan gate, orchestrator SENDS the tmux keystroke after the human approves; never hand it back to the operator (issues #280/#281/#282)
- [L3 broker plan-gate (standard)](l3-broker-plan-gate.md) — operator's DEFAULT for L3: orchestrator presents each plan in-session with options, human decides HERE, orchestrator sends the keystroke back to the golem — never route the operator to attach a TTY for routine approval
- [Review-wedge root cause](review-wedge-root-cause.md) — golems wedge in unbounded ship-issue reviews (3× in batch-5); root cause = #307 wall-time bound is PROSE not code + stale /opt install; filed #327; includes takeover recipe + wedge-vs-healthy-slow diagnostic
- [Wall-timeout decision helper](wall-timeout-decision-helper.md) — #327 fix: TaskStop is model-only so full mechanical enforcement is impossible; mechanized the DECISION via workflow-wall-timeout.sh (called by the 3 prose sites), model still issues TaskStop
- [Release process](release-process.md) — how to cut a repo-level vX.Y.Z release; what containers#608's LIBRARIAN_REF pins to
- [Flaky golem-gate-watch test](flaky-golem-gate-watch-test.md) — root-caused to GIT_DIR leak from push hook; fixed in PR #62
- [golem-gate-watch host leak](golem-gate-watch-host-leak.md) — liveness test fails locally when real host golem sessions leak into its sweep ("advancing"); pre-existing, env-only, push --no-verify, CI passes
- [Conform scope enum](conform-scope-enum.md) — fix(review): is rejected; use fix(workflow): etc. — the skill's generic scope ≠ this repo's enum
- [Skill required_tools vocabulary](skill-required-tools-vocabulary.md) — metadata.yml required_tools are shell-command names; no SKILL.md has a frontmatter tools: field
- [Devcontainer bash_env PATH reset](devcontainer-bash-env-path-reset.md) — /etc/bash_env resets $PATH on non-interactive bash; unset BASH_ENV for PATH-stub tests
- [Autonomy vs plan-gate flags](autonomy-vs-plangate-flags.md) — --autonomous (autonomy) vs --skip-plan/--force-auto (plan-gate) are orthogonal; 3 unrelated --auto spellings never to rename
- [Release from worktree (git-cliff)](release-from-worktree-gitcliff.md) — git-cliff scopes to cwd; release.sh needs --include-path or it silently wipes CHANGELOG from a worktree
- [Two-runtime model](two-runtime-model.md) — workflow.js engine is sandboxed (no shell/fs); only Bash-tool subagents reach host tools; Claude Code bundles no general-purpose runtime
- [core.bare misconfig](core-bare-misconfig.md) — "must be run in a work tree" error = stray core.bare=true in .git/config; set it false (also masks real git status)
- [workflow.js no module system](workflow-js-no-module-system.md) — harnesses can't be split into importable schemas.js/prompt-utils.js (no import/require/fs); audit keeps re-filing impossible "god module" extractions (issues #90, #91); remedy = entry-point functions + header banner (PR #114 worked precedent)
- [workflow.js no clock](workflow-js-no-clock.md) — harnesses can't self-bound in wall-time (Date.now/timers/AbortSignal all absent; spinning agent doesn't advance budget); bound at the CALLER (background invoke → poll → TaskStop → recover-journal-partials.sh). #224/PR #307; #306 extends to other callers
- [git-cliff checksum is sha512](git-cliff-checksum-sha512.md) — git-cliff releases ship per-asset .tar.gz.sha512 (not .sha256) + .sig; verify against the published sibling
- [worktree-new seeds ~/.claude.json](worktree-new-seeds-home-claude-json.md) — worktree-new.sh transitively writes $HOME/.claude.json via seed-worktree-trust.sh; sandbox tests must override HOME
- [pre-review-gates project root](pre-review-gates-project-root.md) — pre-review-gates.sh resolves _PROJECT_ROOT via git rev-parse; testing its scan categories needs a GIT_*-scrubbed git sandbox + .py/.js fixtures
- [Test workflow.js pure helpers](test-workflow-js-pure-helpers.md) — slice harness source before first orchestration stmt + eval prefix in new Function (can't import a top-level-await module); tests/validate-workflow-helpers.mjs, PR #121
- [Codebase-audit prescan location](codebase-audit-prescan-location.md) — Step 2.5 patterns.sh prose lives in orchestration-protocol.md (post-#106), checker.md owns execution; #107 added the source-aware integrity gate; beware stale local main
- [Workflow agentType namespacing](workflow-agenttype-namespacing.md) — Workflow tool agent() wants namespaced <plugin>:<name>, Agent tool wants bare — opposite; FIXED #126/PR #128 (harnesses namespaced + lint-skills-agents.sh gate enforces it); fallback = Agent tool with bare name
- [ship-issue rename rationale](ship-issue-rename-rationale.md) — the ship skill is ship-issue (not next-issue-ship) on purpose; moved out of next-issue* prefix to fix a `nex` autocomplete collision; don't rename it back
- [typos gate blocks push](typos-gate-blocks-push.md) — the `typos` pre-push hook fails the whole push on any misspelling in touched files, including pre-existing ones adjacent to your edits
- [cosign bundle format](cosign-bundle-format.md) — release.yml signing must use --bundle (.sigstore.json); cosign 3.x ignores --output-signature/-certificate; v0.4.0 was first release to run cosign, publish job broke (PR #153)
- [mjs coverage c8 excludes tests](mjs-coverage-c8-excludes-tests.md) — c8 excludes tests/ by default so covering tests/*.mjs needs --exclude override; NODE_V8_COVERAGE + c8 report merges multiple entry points (#186)
- [pre-review-gates needs filelist](pre-review-gates-needs-filelist.md) — pre-review-gates.sh takes changed files as positional args; bare call errors with Usage
- [coverage two surfaces](coverage-two-surfaces.md) — patterns.py Codecov number comes from coverage-python.sh's own corpus, not the behavioral test gates; extend both to lift coverage + assert behavior (#204)
- [autonomy-resolver script](autonomy-resolver-script.md) — autonomy-resolve.{py,sh} is the single source of truth for the L1-L4 gate-disposition table (#190); skills call it, don't re-derive; killed the effort-vs-level drift
- [check-ai-config bloat scan](check-ai-config-bloat-scan.md) — how to run the ai-file-bloat scanner locally (path-list arg); raw-line thresholds per file type (SKILL.md 300/500)
- [Deprecated autonomy flags removed](deprecated-autonomy-flags-removed.md) — #215 killed --autonomous/--auto/--plan-gate/--force-auto/env; --level N is the sole dial; resolver still EMITS plan_gated/perm_mode (runtime), only state-file mirrors dropped
- [rumdl nested sublist under numbered](rumdl-nested-sublist-under-numbered.md) — rumdl MD077 autofix dedents a nested dash-sublist out of its numbered/lettered parent bullet; use flat bold-prose sub-cases instead (hit editing ship-issue execute-protocol.md, #225)
- [golem-launch version-skew guard](golem-launch-version-skew-guard.md) — golem-launch.sh refuses dispatch on plugin version skew (#230/PR #237); "unknown" registry sentinel + unset $HOME must skip, not fire
- [/usr/bin hardcoding in golem scripts](usr-bin-hardcoding-golem-scripts.md) — golem scripts hardcode /usr/bin/<tool> (exit 127 off-/usr/bin); fix = `command <tool>`; #228/PR #241 fixed only worktree-new.sh, siblings + config.sh:repo_root still affected
- [golem-notify feed can't classify forks](golem-notify-feed-cant-classify-forks.md) — #321/PR #342: in-turn AskUserQuestion forks go via canUseTool not Notification, so the feed channel structurally can't classify them; only the ESCALATION: path + pane channel; documented-and-closed, don't re-file
- [repo_root submodule superproject](repo-root-submodule-superproject.md) — #324/PR #335: repo_root() placed worktrees in .git/modules inside a submodule; fix probes --show-superproject-working-tree first (detector+value, scrubbed); follow-ups #336/#337 done, #338 done (PR #364), #365 done (PR #373)
- [worktree-new test coverage matrix](worktree-new-test-coverage-matrix.md) — the deliberate worktree-new.sh × submodule × taint test matrix in validate-golem-scripts.sh; which cell each test fills; #365 (PR #373) closed the last cell — matrix COMPLETE; also the per-scrub-VAR behavioral coverage (#355/#376: file-injection + invalid-bool discriminators, mutation-level hooksPath); shared _make_super_with_submodule fixture + printf -v out-var hand-roll rules
- [Read issue comments not just body](next-issue-read-issue-comments.md) — gh issue view --json body omits comments; follow-up comments fold in requirements (missed on #328, pre-PR review caught it); fetch --comments in Phase 2
- [jq empty vs jq -e for JSON validity](jq-validate-empty-vs-e.md) — validity check = `jq empty` (parse-only); `jq -e .` misreports valid scalars false/null as invalid (keys on truthiness); surfaced hardening harness assert_valid_json #253/PR #351, self-test follow-up #352
- [Umbrella issue Closes vs Contributes](umbrella-issue-closes-vs-contributes.md) — shipping one decomposed slice of a multi-part issue → use "Contributes to #N" + file a follow-up, never "Closes #N" (auto-closes umbrella); ship-issue scope-drift review caught this on #243/PR #349, follow-up #348
- [golem-watch trap signal testing](golem-watch-trap-signal-testing.md) — #254/PR #358: covering the trap's INT/TERM arms needs a STRUCTURAL grep (no behavioural test catches "dropped INT TERM"); #359 SHIPPED a setsid-free BEHAVIOURAL test via the INT-ignoring-pane-worker trick (worker death proves cleanup ran, non-tautological) + self-gating capability probe (skips on CI instead of flaking); #360 SHIPPED (PR #380) readiness-poll of the natural-exit sleep + widened reap margin; deferred coverage → #381
- [Mode 3 takeover branch naming](mode3-takeover-branch-naming.md) — Mode 2 golem branch=feature/issue-{N}/session golem-{N}/tmux-kill teardown vs Mode 3 branch=agent{N}/session claude/docker-compose stop-rm; #370 (PR #385) made the slow-review takeover recipe + pre-kill PR check + monitor-protocol restatement all mode-aware (pre-kill --head bug caught by adversarial review, not plan)
- [Source-detector gate (#348 slice A)](source-detector-gate.md) — new tests/validate-source-detectors.sh + coverage-python.sh corpus drove the 3 review-audit ports to 100% (check-security/code-health/ai-config); fixed missing-api whole-line private-detection bug (name-anchor, both impls); 1 TOCTOU pragma; slice B = 6 dev-core loop-*/drift ports → follow-up
- [Loop-detector gate (#348 slice B)](loop-detector-gate.md) — #384: new tests/validate-loop-detectors.sh + coverage corpus drove the 6 dev-core loop-*/drift ports to 100% (TOTAL 89→100%, umbrella #348 done); gotchas = tested SKIP_GLOBS *test* whole-path (name dir probe/), OSError arms need unreadable siblings; deleted dead _next_nonblank helper + 1 unreachable pragma; just lint fails on pre-existing memory .md debt only
- [Token-scrape transcript dedup](token-scrape-transcript-dedup.md) — #371 (PR #393): golem-status TOP-LEVEL TOKENS signal scrapes ~/.claude/projects/<slug> transcript; MUST dedup by message.id (CC writes 1 line/content-block, same output_tokens → naive sum ~2.7x too high); isSidechain==false = top-level; Mode 2 only (#390 = Mode 3 HTTP-post, #392 = coverage); frozen-anchor test needs mutation-verify not substring
- [Broker-inbox gate resolution](broker-inbox-gate-resolution.md) — #227 slice 1: golem-inbox.sh reverse channel relays escalation/dead-end decisions back down (gate-id embedded in feed message, no schema change); DATA-ONLY, plan-approval excluded by #29; consume = bounded read + NO-DECISION re-invoke loop; review caught octal-hang (10# normalize) + jq-doc-abort-on-torn-line (per-line fromjson?)
- [#390 ship CI-hang dead-end](issue-390-ship-ci-hang-deadend.md) — #390 (Mode-3 container token consume) → PR #440, rebased clean, DEAD-ENDED at merge gate on the run-all.sh CI runner hang (#441, fix #442 unmerged): quality-gates cancelled at 20min job timeout 3× while the suite passes locally in 74s; merge invariant → parked for human, issue keeps pr-pending; adversarial review's deferrables (anchor allowlist, corrupt/partial-POST tests, scope-drift wording) folded INTO the PR; resume = rebase after #442 lands or human-merge on a healthy runner
- [#392 token-signal coverage](issue-392-token-signal-coverage.md) — SHIPPED PR #413 (2026-07-19): 7 render-path tests for golem-status/scrape token signal (#371 follow-up), coverage+docs only; iso_ago helper seeds ~20s/~130s (not 59/60); curated PATH shim for no-jq test; pre-PR review caught HIGH tautology `grep -Evq` (per-line inversion = unconditionally true; use `! grep -q`); README got scrape row + CLAUDE_PROJECTS_DIR
- [#426 harness rm -rf](issue-426-harness-rm-rf.md) — CRITICAL/large: read-only reviewer subagents can run destructive shell (rm -rf) against the LIVE tree; ship-issue review deleted real .worktrees/ via unresolved `..` from a subdir (git-common-dir returns relative path); class bug across ship-issue/codebase-audit/orchestrate workflow.js READONLY prompts + every Bash-capable review agent; fix = ban destructive shell + mktemp -d sandbox + tool-level guardrail + audit surface; HELD for interactive (you+me), NOT autonomous golem — a golem fixing its own reviewer is a footgun
- [Marketplace source = reaped worktree](marketplace-source-reaped-worktree.md) — librarian marketplace got registered against a golem worktree (.worktrees/issue-422); reaping it post-merge → cache-miss → all librarian plugins fail to load → NEWEST dispatched golem DOA (Unknown command: /workflow:next-issue) while older siblings (plugin already resident) work; FIX = `claude plugin marketplace add /workspace/librarian` (stable main checkout) then re-dispatch; never marketplace-add from a worktree cwd
- [Orchestrate session 2026-07-19 batch](orchestrate-session-2026-07-19-batch.md) — live burndown: 8 PRs landed (417/405/409/415/412/422/436/419); found+fixed 3 infra bugs (426 harness rm-rf HELD interactive, marketplace-reaped-worktree DOA, 436 liveness-leak fixed HONESTLY no --no-verify); #397 agnix CI-timeout OPEN (correct code, GitHub runner 15min hang, not local-reproducible); /opt now 0.7.0 (launcher --level flag + auth stopgap retired); tracks T0 DONE, T1/T2/T3 serial-refilling
- [CI hang: golem-watch group signal](ci-hang-golem-watch-group-signal.md) — multi-PR 15min CI timeout root cause: validate-golem-watch case-4 group-signal (set -m + kill -INT -<pgid>) escapes to run-all.sh on headless x86_64 CI (job control unreliable); NOT reproducible on aarch64 dev host (why it hid); every isolation fix squirmed (dead end); FIX = REMOVE case 4, cases 2+3 already cover the one-line trap (#444/PR#445); diagnostic = run_stage [>>] markers (#441/PR#442, MARKERS-ONLY — timeout wrapper perturbs the signal); GitHub purges timed-out-job logs
