name: check-code-health
version: "1.0"
labels: []
required_tools:
  - name: grep
    purpose: "Pattern matching for code health findings"
    install_hint: "Included in base container image"
required_permissions: []
required_mcps: []
